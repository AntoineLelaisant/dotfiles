#!/bin/sh

yay -S --noconfirm neovim vim-plug xsel python2-pip

pip install neovim
pip2 install --user neovim

yarn global add neovim

ln -sf $(git rev-parse --show-toplevel)/.config/nvim $HOME/.config

nvim +'PlugInstall --sync' +qa
